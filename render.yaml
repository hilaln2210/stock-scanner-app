# Render.com Blueprint - פריסה 24/7
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: stock-scanner
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: "8000"
